# mechanic-shop-api

## Implemented RATE LIMITING for deleting and adding mechanics, this is to prevent adding or deleting too many.
## Implemented CACHING for all of the GET routes, so that the app runs smoother and stores the data, rather than pulling from the database each time.